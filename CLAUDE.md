this is a discord bot similar to homarr but using discord as an interface, it should be modular, having one module per integration.

each integration:
- has state
- has config section in yaml file

integrations:
1. jellyfin
- notify latest movies/episodes/series
2. qbittorrent
- show current torrent status